# Reading-list benchmark fixture

This is a dependency-free Node ESM CLI. Keep changes within the paths named by the active benchmark
prompt. Use `node --test` or the narrower command in that prompt. Do not access a real network or a
real GitHub repository; `gh` is a local deterministic fixture.
