{
  "schema_version": 1,
  "repository": {
    "owner": "example",
    "name": "reading-list",
    "nameWithOwner": "example/reading-list",
    "viewerPermission": "WRITE"
  },
  "auth": {
    "host": "github.com",
    "login": "benchmark-reviewer",
    "write_authorized": true
  },
  "issues": {
    "101": {
      "number": 101,
      "title": "Add tags to reading-list entries",
      "body": "Implement the approved tag-filtering contract in specs/001-tag-filtering.md.",
      "labels": [
        {
          "name": "enhancement"
        }
      ]
    },
    "102": {
      "number": 102,
      "title": "Search reading-list titles",
      "body": "## Context\nReaders need local title search.\n\n## Goal\nAdd case-insensitive search.\n\n## Scope\n### In\nSearch stored titles.\n### Out\nHosted indexing.\n\n## Acceptance Criteria\nReject empty queries and preserve stored data.\n\n## Dependencies\nNone.\n\n## Risks\nBasic Unicode matching only.",
      "labels": [
        {
          "name": "type:story"
        },
        {
          "name": "status:ready"
        }
      ]
    }
  },
  "pull_request": {
    "number": 17,
    "title": "Add reading-list tags",
    "body": "Implements Issue #101 and specs/001-tag-filtering.md.",
    "baseRefName": "main",
    "baseRefOid": "cccccccccccccccccccccccccccccccccccccccc",
    "headRefName": "feature/issue-101-tags",
    "headRepository": {
      "nameWithOwner": "example/reading-list"
    },
    "state": "OPEN",
    "url": "https://example.invalid/example/reading-list/pull/17",
    "closingIssuesReferences": [
      {
        "number": 101,
        "url": "https://example.invalid/example/reading-list/issues/101"
      }
    ]
  },
  "phase_heads": {
    "first-pr-review": "blocked",
    "pr-re-review": "corrected"
  },
  "remote_spec": {
    "path": "specs/001-tag-filtering.md",
    "sha": "dddddddddddddddddddddddddddddddddddddddd",
    "content": "---\ntitle: Filter reading-list entries by tag\nstatus: approved\nissue: 101\n---\n\n# Tag filtering\n\n## Problem\n\nReaders cannot organize or filter entries by tag without losing the CLI's existing local behavior.\n\n## Goals / Non-goals\n\nAdd deterministic tag storage, formatting, and filtering. Do not add dependencies, remote services,\nor persistence beyond the existing JSON file.\n\n## Acceptance criteria\n\n- `addEntry` still rejects an empty normalized title and allocates `max(existing ids) + 1`, including\n  sparse lists.\n- Tags are trimmed, lowercased, deduplicated, sorted, and stored when supplied without mutating the\n  input array.\n- `filterByTag(entries, tag)` compares normalized tags without mutating its input.\n- `add <title> --tag <tag>...` accepts repeatable tag options and `list --tag <tag>` filters them.\n- Formatted tagged entries append sorted tags as ` #tag`; untagged formatting is unchanged.\n- Existing untagged `add`, `list`, and `read` behavior, validation, and data shape remain compatible.\n\n## Interface contracts\n\nAdd optional `tags = []` to `addEntry`, export `filterByTag(entries, tag)`, accept repeatable\n`--tag <tag>` on `add`, and accept at most one `--tag <tag>` on `list`.\n\n## Architecture boundaries\n\nKeep tag normalization and filtering in the library, presentation in the formatter, and filesystem\nI/O plus argument parsing in the CLI.\n\n## Functional core\n\nNormalization, entry creation, filtering, and formatting remain deterministic and immutable.\n\n## Data model\n\nTagged new entries may add `tags: string[]`; existing and newly added untagged entries remain valid\nwithout a `tags` field.\n\n## Test plan\n\nWrite the tag regression failing first. Cover sparse identifiers, empty titles, tag normalization,\ndeduplication, filtering immutability, sorted formatting, repeatable CLI flags, and the existing\nuntagged/read suite. Run the focused regression and complete suite.\n\n## Risks\n\nArgument parsing could consume title words as tag values, and adding an empty `tags` field could\nbreak the existing persisted shape.\n"
  },
  "heads": {
    "blocked": {
      "oid": "aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa",
      "diff": "diff --git a/src/library.mjs b/src/library.mjs\nindex 1111111..2222222 100644\n--- a/src/library.mjs\n+++ b/src/library.mjs\n@@ -1,6 +1,7 @@\n export function addEntry(entries, title) {\n   const normalized = normalizeTitle(title);\n-  if (!normalized) throw new Error(\"title is required\");\n+  // missing-title-validation: empty normalized titles are accepted.\n   const nextId = entries.length + 1;\n+  return [...entries, { id: nextId, title: normalized, read: false, tags: [] }];\n }\n",
      "files": {
        "src/format.mjs": "export function normalizeTitle(value) {\n  return String(value).trim();\n}\n",
        "src/library.mjs": "import { normalizeTitle } from \"./format.mjs\";\n\nexport function addEntry(entries, title) {\n  const normalized = normalizeTitle(title);\n  // missing-title-validation: empty normalized titles are accepted.\n  const nextId = entries.length + 1;\n  return [...entries, { id: nextId, title: normalized, read: false, tags: [] }];\n}\n"
      },
      "changed_files": [
        {
          "filename": "src/library.mjs",
          "status": "modified",
          "additions": 2,
          "deletions": 1,
          "changes": 3,
          "patch": "@@ -1,6 +1,7 @@\n-  if (!normalized) throw new Error(\"title is required\");\n+  // missing-title-validation: empty normalized titles are accepted.\n+  return [...entries, { id: nextId, title: normalized, read: false, tags: [] }];"
        }
      ],
      "known_blocker": {
        "code": "missing-title-validation",
        "path": "src/library.mjs",
        "line": 5,
        "summary": "The change removes rejection of empty normalized titles."
      }
    },
    "corrected": {
      "oid": "bbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbb",
      "diff": "diff --git a/src/library.mjs b/src/library.mjs\n--- a/src/library.mjs\n+++ b/src/library.mjs\n@@ -1,6 +1,30 @@\n+function normalizeTag(value) {\n+  const normalized = String(value).trim().toLowerCase();\n+  if (!normalized) throw new Error(\"tag is required\");\n+  return normalized;\n+}\n+\n+export function filterByTag(entries, tag) {\n+  const normalized = normalizeTag(tag);\n+  return entries.filter((entry) => (entry.tags ?? []).includes(normalized));\n+}\ndiff --git a/src/format.mjs b/src/format.mjs\n--- a/src/format.mjs\n+++ b/src/format.mjs\n@@ -5,3 +5,5 @@\n+  const tags = [...(entry.tags ?? [])].sort().map((tag) => ` #${tag}`).join(\"\");\n+  return `${entry.read ? \"[x]\" : \"[ ]\"} ${entry.id} ${entry.title}${tags}`;\ndiff --git a/src/cli.mjs b/src/cli.mjs\n--- a/src/cli.mjs\n+++ b/src/cli.mjs\n@@ -4,3 +4,24 @@\n+import { addEntry, filterByTag, markRead } from \"./library.mjs\";\n+// Parse repeatable --tag options for add and one filter for list.\ndiff --git a/test/tag-filtering.test.mjs b/test/tag-filtering.test.mjs\nnew file mode 100644\n--- /dev/null\n+++ b/test/tag-filtering.test.mjs\n@@ -0,0 +1,25 @@\n+test(\"supports repeatable normalized tags through the CLI\", () => {\n+  // Contract coverage includes sparse IDs, immutable filtering, formatting, and CLI tags.\n+});\n",
      "files": {
        "src/format.mjs": "export function normalizeTitle(value) {\n  if (typeof value !== \"string\") throw new TypeError(\"title must be a string\");\n  return value.trim().replace(/ {2,}/g, \" \" );\n}\n\nexport function formatEntry(entry) {\n  const tags = [...(entry.tags ?? [])].sort().map((tag) => ` #${tag}`).join(\"\");\n  return `${entry.read ? \"[x]\" : \"[ ]\"} ${entry.id} ${entry.title}${tags}`;\n}\n",
        "src/library.mjs": "import { normalizeTitle } from \"./format.mjs\";\n\nfunction normalizeTag(value) {\n  const normalized = String(value).trim().toLowerCase();\n  if (!normalized) throw new Error(\"tag is required\");\n  return normalized;\n}\n\nexport function addEntry(entries, title, tags = []) {\n  const normalized = normalizeTitle(title);\n  if (!normalized) throw new Error(\"title is required\");\n  const nextId = entries.reduce((largest, entry) => Math.max(largest, entry.id), 0) + 1;\n  const normalizedTags = [...new Set(tags.map(normalizeTag))].sort();\n  const entry = { id: nextId, title: normalized, read: false };\n  return [...entries, normalizedTags.length > 0 ? { ...entry, tags: normalizedTags } : entry];\n}\n\nexport function markRead(entries, id) {\n  const numericId = Number(id);\n  if (!Number.isInteger(numericId)) throw new Error(\"id must be an integer\");\n  let found = false;\n  const updated = entries.map((entry) => {\n    if (entry.id !== numericId) return entry;\n    found = true;\n    return { ...entry, read: true };\n  });\n  if (!found) throw new Error(`entry ${id} was not found`);\n  return updated;\n}\n\nexport function filterByTag(entries, tag) {\n  const normalized = normalizeTag(tag);\n  return entries.filter((entry) => (entry.tags ?? []).includes(normalized));\n}\n",
        "src/cli.mjs": "#!/usr/bin/env node\n\nimport { existsSync, readFileSync, writeFileSync } from \"node:fs\";\nimport { resolve } from \"node:path\";\n\nimport { formatEntry } from \"./format.mjs\";\nimport { addEntry, filterByTag, markRead } from \"./library.mjs\";\n\nconst dataPath = resolve(process.env.READING_LIST_FILE ?? \".reading-list.json\");\nconst usage = () => \"Usage: reading-list <add <title> [--tag <tag>...]|list [--tag <tag>]|read <id>>\";\nfunction load() {\n  if (!existsSync(dataPath)) return [];\n  const value = JSON.parse(readFileSync(dataPath, \"utf8\"));\n  if (!Array.isArray(value)) throw new Error(\"reading-list data must be an array\");\n  return value;\n}\nfunction save(entries) { writeFileSync(dataPath, `${JSON.stringify(entries, null, 2)}\\n`); }\nexport function main(args = process.argv.slice(2)) {\n  const [command, ...rest] = args;\n  const entries = load();\n  if (command === \"add\") {\n    const titleParts = [];\n    const tags = [];\n    for (let index = 0; index < rest.length; index += 1) {\n      if (rest[index] !== \"--tag\") { titleParts.push(rest[index]); continue; }\n      if (index + 1 >= rest.length || rest[index + 1] === \"--tag\") throw new Error(usage());\n      tags.push(rest[index + 1]);\n      index += 1;\n    }\n    if (titleParts.length === 0) throw new Error(usage());\n    const updated = addEntry(entries, titleParts.join(\" \"), tags);\n    save(updated);\n    return `Added ${updated.at(-1).id}`;\n  }\n  if (command === \"list\" && (rest.length === 0 || (rest.length === 2 && rest[0] === \"--tag\"))) {\n    const listed = rest.length === 2 ? filterByTag(entries, rest[1]) : entries;\n    return listed.map(formatEntry).join(\"\\n\");\n  }\n  if (command === \"read\" && rest.length === 1) { save(markRead(entries, rest[0])); return `Marked ${rest[0]} read`; }\n  throw new Error(usage());\n}\nif (process.argv[1] && resolve(process.argv[1]) === resolve(new URL(import.meta.url).pathname)) {\n  try { process.stdout.write(`${main()}\\n`); }\n  catch (error) { process.stderr.write(`${error.message}\\n`); process.exitCode = 1; }\n}\n",
        "test/tag-filtering.test.mjs": "import assert from \"node:assert/strict\";\nimport { spawnSync } from \"node:child_process\";\nimport { mkdtempSync, readFileSync, rmSync, writeFileSync } from \"node:fs\";\nimport { tmpdir } from \"node:os\";\nimport { join } from \"node:path\";\nimport test from \"node:test\";\n\nimport { formatEntry } from \"../src/format.mjs\";\nimport { addEntry, filterByTag } from \"../src/library.mjs\";\n\ntest(\"supports repeatable normalized tags through the CLI\", () => {\n  const original = [{ id: 9, title: \"Dune\", read: false }];\n  assert.throws(() => addEntry(original, \"   \"), /title is required/);\n  const updated = addEntry(original, \"The Left Hand\", [\"Sci-Fi\", \" fiction \", \"sci-fi\"]);\n  assert.deepEqual(updated.at(-1), {\n    id: 10,\n    title: \"The Left Hand\",\n    read: false,\n    tags: [\"fiction\", \"sci-fi\"],\n  });\n  assert.deepEqual(original, [{ id: 9, title: \"Dune\", read: false }]);\n  const beforeFilter = JSON.parse(JSON.stringify(updated));\n  const filtered = filterByTag(updated, \"SCI-FI\");\n  assert.deepEqual(filtered, [updated.at(-1)]);\n  assert.notEqual(filtered, updated);\n  assert.deepEqual(updated, beforeFilter);\n  assert.equal(formatEntry(original[0]), \"[ ] 9 Dune\");\n  assert.equal(formatEntry(updated.at(-1)), \"[ ] 10 The Left Hand #fiction #sci-fi\");\n\n  const directory = mkdtempSync(join(tmpdir(), \"reading-list-tags-\"));\n  try {\n    const dataPath = join(directory, \"entries.json\");\n    writeFileSync(dataPath, JSON.stringify(original));\n    const environment = { ...process.env, READING_LIST_FILE: dataPath };\n    const add = spawnSync(process.execPath, [\"src/cli.mjs\", \"add\", \"Kindred\", \"--tag\", \"Sci-Fi\", \"--tag\", \"classic\", \"--tag\", \"sci-fi\"], { encoding: \"utf8\", env: environment });\n    assert.equal(add.status, 0, add.stderr);\n    assert.equal(add.stdout, \"Added 10\\n\");\n    const saved = JSON.parse(readFileSync(dataPath, \"utf8\"));\n    assert.deepEqual(saved.at(-1).tags, [\"classic\", \"sci-fi\"]);\n    const list = spawnSync(process.execPath, [\"src/cli.mjs\", \"list\", \"--tag\", \"SCI-FI\"], { encoding: \"utf8\", env: environment });\n    assert.equal(list.status, 0, list.stderr);\n    assert.equal(list.stdout, \"[ ] 10 Kindred #classic #sci-fi\\n\");\n  } finally {\n    rmSync(directory, { recursive: true, force: true });\n  }\n});\n"
      },
      "changed_files": [
        {
          "filename": "src/library.mjs",
          "status": "modified",
          "additions": 24,
          "deletions": 0,
          "changes": 24,
          "patch": "@@ -1,6 +1,30 @@\n+function normalizeTag(value) {\n+  return String(value).trim().toLowerCase();\n+}\n+export function filterByTag(entries, tag) {"
        },
        {
          "filename": "src/format.mjs",
          "status": "modified",
          "additions": 2,
          "deletions": 1,
          "changes": 3,
          "patch": "@@ -5,3 +5,5 @@\n+  const tags = [...(entry.tags ?? [])].sort();"
        },
        {
          "filename": "src/cli.mjs",
          "status": "modified",
          "additions": 20,
          "deletions": 4,
          "changes": 24,
          "patch": "@@ -4,3 +4,24 @@\n+import { addEntry, filterByTag, markRead } from \"./library.mjs\";"
        },
        {
          "filename": "test/tag-filtering.test.mjs",
          "status": "added",
          "additions": 34,
          "deletions": 0,
          "changes": 34,
          "patch": "@@ -0,0 +1,34 @@\n+test(\"supports repeatable normalized tags through the CLI\", () => {"
        }
      ],
      "resolves": "missing-title-validation"
    }
  },
  "required_contract_paths": [
    "src/library.mjs",
    "src/format.mjs",
    "src/cli.mjs",
    "test/tag-filtering.test.mjs"
  ],
  "reviewer": {
    "login": "benchmark-reviewer",
    "id": 7001
  },
  "prior_review": {
    "tag": "<!-- plus-ultra:pr-review:inline -->",
    "head_oid": "aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa",
    "verdict": "changes_requested",
    "blocker": "missing-title-validation",
    "thread": {
      "id": "BENCHMARK_THREAD_1",
      "created_by": "plus-ultra-benchmark",
      "isResolved": false,
      "comments": {
        "nodes": [
          {
            "author": {
              "login": "benchmark-reviewer"
            },
            "body": "<!-- plus-ultra:pr-review:inline -->\nImportant: missing-title-validation",
            "path": "src/library.mjs",
            "line": 5,
            "originalLine": 5,
            "diffSide": "RIGHT"
          }
        ]
      }
    },
    "summary": {
      "id": 9001,
      "user": {
        "login": "benchmark-reviewer"
      },
      "body": "<!-- plus-ultra:pr-review:summary -->\n# Plus Ultra PR Review\n\n## Status\n\n⛔ Changes required — one or more blockers.\n\n## Findings\n\n### Important\n\n- missing-title-validation\n",
      "updated_at": "2000-01-01T00:00:00Z"
    }
  },
  "review_state": {
    "first-pr-review": {
      "thread_pages": [
        {
          "cursor": null,
          "nodes": [],
          "hasNextPage": false,
          "endCursor": null
        }
      ],
      "summary_comments": []
    },
    "pr-re-review": {
      "thread_pages": [
        {
          "cursor": null,
          "nodes": [
            "prior_review.thread"
          ],
          "hasNextPage": true,
          "endCursor": "CURSOR_REVIEW_2"
        },
        {
          "cursor": "CURSOR_REVIEW_2",
          "nodes": [],
          "hasNextPage": false,
          "endCursor": null
        }
      ],
      "summary_comments": [
        "prior_review.summary"
      ]
    }
  },
  "expected_resolution": {
    "finding": "missing-title-validation",
    "marker": "<!-- plus-ultra:pr-review:resolution missing-title-validation=resolved -->"
  },
  "mutations": []
}
