Benchmark protocol: `spec-approval-v1`.

I explicitly approve the complete Search reading-list titles technical contract from the preceding
turn. Use `plus-ultra:spec status 002 approved` to record that approval by changing only the
frontmatter status in `specs/002-search-reading-list.md`. Preserve the contract content and report
the durable artifact path. Do not use `gh` or any network capability.
