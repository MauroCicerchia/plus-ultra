---
description: Start a new product — product discovery, an approved brief, and an initialized repository.
argument-hint: "[describe the idea]"
---

Arguments: `$ARGUMENTS`

Follow the `plus-ultra:new-project` skill. Run `plus-ultra:product` discovery first and wait for
explicit approval of `docs/product.md` before choosing a stack or creating any file. If the product
has a real user interface, run design discovery next and wait for approval of `DESIGN.md` too.
