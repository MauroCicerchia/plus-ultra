---
description: Make a GitHub Issue ready to implement — resolve product ambiguity, settle significant UI direction, and write acceptance criteria.
argument-hint: "<issue-number>"
---

Arguments: `$ARGUMENTS`

Accept exactly one positive Issue number. Otherwise explain the valid form and stop without a
GitHub write.

Follow the `plus-ultra:refine-issue` skill. Present the complete proposal and wait for explicit
confirmation before editing the Issue, its labels, its milestone, or its relationships.
